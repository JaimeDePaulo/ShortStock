rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Regras temporárias para teste conforme solicitado pelo utilizador
    // TODO: Endurecer após validar o fluxo de autenticação
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
